{
  "name": "bloglist-backend",
  "version": "1.0.0",
  "description": "An exercise for Fullstack Open 2022. This is a simple application that lets users save information about interesting blogs that find on the internet.",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "lint": "eslint .",
    "test": "NODE_ENV=test jest --verbose --runInBand"
  },
  "author": "Marge C.",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "mongoose": "^6.3.1",
    "nodemon": "^2.0.15"
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "jest": "^28.0.1",
    "mongodb-memory-server": "^8.5.2",
    "supertest": "^6.2.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
